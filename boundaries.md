# 诚实边界（公开）

> ShardVase 的安全主张是**有测试依据的、有限度的**。本页公开我们已知的
> 边界与缓解措施——比营销承诺更可靠。近期攻防结论见
> [attack-record](attack-record.md)。

## 1. 已知边界与缓解

| # | 边界（实证） | 含义 | 缓解路径 |
|---|--------------|------|----------|
| B1 | 安装文件可写 → 可替换内嵌公钥自签授权（D2a 实证）| 攻击者能改 exe 就能重签 | 安装目录 ACL 防写（部署要求）；防篡改只防"在线篡改不改文件"类 |
| B2 | 到期控制依赖本地时钟 → 可回拨复活性 | expires 是"防君子" | 在线时间锚点（商业版路线）；或仅用于试用期控制 |
| B3 | 完整内存转储可取证明文窗口（活动 VM 周期内）| 静态取证成本随时间与加密窗口显著上升 | 减少明文窗口（D2b 加密粒度为调用级）；模拟器还原需多镜像+N5 会话值 |
| B4 | 画质/取证类攻击者用模拟器可还原单镜像语义 | 单镜像还原不足以通过（需级联+N5 会话值）| 多 VM 拆分 + 会话级随机期望值 |
| B5 | 用户态无法防内核级攻击 | 驱动级 dump/hook 不在覆盖 | 反调试对内核调试器有检测位；深度对抗需驱动（未提供）|
| B6 | 撤销非实时 | 离线黑名单（revoke.blob）下发有延迟 | 在线查询端点可选（默认关闭）|

## 2. 我们不承诺的
- 绝不承诺"无法破解/永不破解"；
- 不承诺对所有杀软/企业策略零误报（会持续收敛并配合申诉）；
- 不承诺跨语言运行时（当前 C/MSVC x64）。

## 3. 我们的验证方式（可复现）
- 攻防演练：7 轮完整轮次 + 4 组矩阵（结果归档，见 attack-record.md）；
- 干净环境零误报基线（antidebug selftest）；
- 授权链路：正例/换机/篡改/过期/无授权五路径自动化矩阵。

### 显式破防与上述边界的关系

演练累计 **BLOCKED 54 / PARTIAL 58 / 显式破防 5 / SKIP 5**。
**5 项破防的逐项明细（含前置条件与缓解路径）列在
[攻防记录](attack-record.md#显式破防的-5-项全部公开附前置条件与缓解路径)里。**

它们与上表边界的关系：

| 破防项 | 对应边界 |
|--------|----------|
| D5 DLL 劫持 / L3 公钥替换 | **B1**（安装文件可写）—— 部署 ACL 缓解 |
| L4 时钟回拨 | **B2**（到期依赖本地时钟）—— 需在线时间锚点 |
| S5 台账可改写 / V3 CSV 注入 | 工具层/运维层，已由哈希链与字段卫生缓解 |

> 换言之：**5 项破防没有"未知项"** —— 全部落在已公开的边界或已缓解的
> 工具层问题内。这是我们敢公开它们的原因。

## 4. 报告与响应
发现我们边界外的破防手法 → 请开 Issue（[漏洞报告](https://github.com/auui-LDAH/ShardVase-docs/issues/new?template=security.yml)），
我们会验证、记录、并在能力页与边界页更新——**不删帖、不装看不见**。